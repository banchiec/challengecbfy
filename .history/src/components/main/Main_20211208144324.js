import { ShoppingCart } from "../shoppingCart.js/ShoppingCart"

export default function Main(props) {
    console.log(props)
    return (
        <main>
            <ShoppingCart />
        </main>
    )
}
