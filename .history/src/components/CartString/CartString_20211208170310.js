export const CartString = () => {
    const {
        content: {
            productCode,
            name,
            count,
            imageName,
            price
        },
        scan,
        cost
    } = props
    return (
        <div>

        </div>
    )
}
