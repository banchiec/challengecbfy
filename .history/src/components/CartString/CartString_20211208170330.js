export const CartString = (props) => {
    const {
        content: {
            productCode,
            name,
            count,
            imageName,
            price
        },
        scan,
        cost
    } = props
    return (
        <div>

        </div>
    )
}
