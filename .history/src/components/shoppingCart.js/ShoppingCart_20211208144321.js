import { UniqueId } from 'lodash'

export const ShoppingCart = (props) => {
    return (
        <div key={UniqueId}>
            {
                // console.log(props)
            }
        </div>
    )
}
